---
import { changeLanguage } from "i18next";

changeLanguage("de");
---

<script is:inline>
  if (/^de\b/.test(navigator.language)) {
    window.location.pathname = `/de/home`;
    //const lang = "de";
  } else {
    window.location.pathname = `/home`;
    //const lang = "en";
  }
</script>
