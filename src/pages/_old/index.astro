---
import Frontpage from "../../layouts/Frontpage.astro";
import { getSeeds } from "../../config_pocketbase";
import i18next, { t } from "i18next";
import { t as old } from "../../data/lang/de/0";

const seeds = await getSeeds();
---

<Frontpage title="xx" desc="x" t={old}>
  <div slot="main">
    <h1 style="font-size:var(--fs-md);">{t("site.title")}</h1>
    <p>This page is experimental... under construction</p>
    <ul>
      {
        seeds.map((seed) => (
          <li>
            <a href={old.pages.seeds.slug + "/" + seed.slug}>
              {seed.name},{" "}
              {seed.expand.author.firstname + " " + seed.expand.author.lastname}
            </a>
          </li>
        ))
      }
    </ul>

    <h2>Overview lists</h2>
    <ul>
      <li>
        <a href={old.pages.environments.slug}>{old.pages.environments.name}</a>
        <li>
          <a href={old.pages.plants.slug}>{old.pages.plants.name}</a>
        </li>
        <li>
          <a href={old.pages.animals.slug}>{old.pages.animals.name}</a>
        </li>
        <li>
          <a href={old.pages.principles.slug}>{old.pages.principles.name}</a>
        </li>
        <li>
          <a href={old.pages.authors.slug}>{old.pages.authors.name}</a>
        </li>
      </li>
    </ul>
    -->
  </div>
</Frontpage>
